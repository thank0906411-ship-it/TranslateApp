<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingTop="8dp"
        android:paddingEnd="8dp">

        <Spinner
            android:id="@+id/spinnerSourceLang"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1" />

        <!-- 도착어는 항상 SupportedLanguages.DEFAULT_TARGET(한국어)로 고정되어
             선택할 필요가 없으므로, 드롭다운 대신 고정 텍스트로만 보여준다.
             텍스트는 MainActivity가 "→ 한국어" 형태로 채운다. -->
        <TextView
            android:id="@+id/textTargetLangFixed"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:paddingStart="4dp"
            android:paddingEnd="4dp" />

        <!-- Widget.MaterialComponents 스타일은 Button 전용(minWidth 등 텍스트 버튼에
             맞춘 속성)이라 ImageButton에는 적합하지 않아 스타일 없이 배경만
             selectableItemBackgroundBorderless로 지정한다(원 모양 리플, 사각 테두리 없음).
             아래 아이콘 버튼들 모두 같은 이유로 스타일을 생략한다. -->
        <ImageButton
            android:id="@+id/btnHistory"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/btn_history"
            android:padding="8dp"
            android:src="@android:drawable/ic_menu_recent_history"
            android:tint="?android:attr/textColorPrimary" />

        <ImageButton
            android:id="@+id/btnCheckUpdate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/btn_check_update"
            android:padding="8dp"
            android:src="@android:drawable/ic_menu_rotate"
            android:tint="?android:attr/textColorPrimary" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp">

        <EditText
            android:id="@+id/editUrl"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="@string/hint_url"
            android:imeOptions="actionGo"
            android:inputType="textUri"
            android:singleLine="true" />

        <Button
            android:id="@+id/btnTranslate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="@string/btn_translate" />

        <!-- 용어집/LLM 설정은 둘 다 "번역 동작 방식을 설정하는" 성격이라 하나의 설정
             메뉴로 묶었다(누르면 PopupMenu로 둘 중 하나를 고름). 기록/업데이트는
             자주 쓰는 독립 기능이라 따로 둔다. -->
        <ImageButton
            android:id="@+id/btnSettings"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/btn_settings"
            android:padding="8dp"
            android:src="@android:drawable/ic_menu_manage"
            android:tint="?android:attr/textColorPrimary" />

    </LinearLayout>

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:indeterminate="true"
        android:visibility="gone" />

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
